# Next Actions

## Current status

Base Agent v2 is currently a proof pack, not a production app.

It should support the Launch Desk Base grant story without creating new spending risk.

## Safe next steps

1. Attach this folder or selected docs to the Base application if a technical note is useful.
2. Link the existing Launch Desk Base demo as the main product demo.
3. Use `docs/grant-submission-base.md` for written application answers.
4. Keep this proof pack separate from WLD.

## Do not do yet

- Do not deploy a smart contract just to look more onchain.
- Do not use mainnet funds before the Base Sepolia path is proven.
- Do not paste real API keys into public docs.
- Do not mix WLD verification language into Base submissions.

## Next build only after confirmation

When ready, the next real build should be:

One x402-protected API route on Base Sepolia that unlocks one Launch Desk output.

That is the smallest meaningful Base-native product step.

